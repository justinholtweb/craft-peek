<?php

/**
 * Application config for the Codeception Yii2 module. Craft builds the whole
 * thing itself from its own app config plus the paths set in tests/_bootstrap.php.
 */

return craft\test\TestSetup::createTestCraftObjectConfig();
